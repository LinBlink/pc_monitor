# PC Monitor settings.
#
# PC_HOST is only the seed: it is what gets shown while the background subnet
# sweep runs, and the fallback if the sweep finds nothing. Discovered devices are
# cached in hosts.txt (delete it to force a clean rescan).
PC_HOST=192.168.2.114

# The port to look for. Every PC you want to monitor must use the same one.
PC_PORT=8765

# Fallback frame rate, used only if the PC's /config.json cannot be read.
# The live value comes from the PC's settings page, so change it there.
STREAM_FPS=8

# How often to re-scan the subnet for PCs running PC Monitor. A PC you switch on
# later shows up in the switcher within this many seconds; the scan only
# interrupts the picture when the list of PCs actually changed.
DISCOVER_EVERY_S=120

# Battery: the level is reported to the PC every BATT_EVERY_S seconds so it can be
# drawn in the header. At or below BATT_LOW_PCT (and not charging) the handheld
# buzzes, at most once every BATT_BUZZ_GAP_S. Set BATT_LOW_PCT=0 to never buzz;
# Onion's own .noVibration switch is honoured either way.
BATT_EVERY_S=60
BATT_LOW_PCT=15
BATT_BUZZ_GAP_S=600

# How often to ask the PC whether it has new AI advice to read out loud. Whether
# there is any, and whether it is worth speaking, is decided on the PC's settings
# page - this is only the polling interval. Only the PC currently on screen is
# asked, so switching devices switches whose advice you hear.
SPEAK_EVERY_S=60
